// Config pubblica del progetto Firebase (Spark, gratuito).
// Non è un segreto: la protezione reale sono le Firestore Security Rules
// (vedi firestore.rules). Vedi README.md per i passaggi di setup.
//
// Sostituisci i valori sotto con quelli mostrati dalla Console Firebase
// in Impostazioni progetto > Le tue app > Configurazione SDK.
export const firebaseConfig = {
  apiKey: 'REPLACE_ME',
  authDomain: 'REPLACE_ME.firebaseapp.com',
  projectId: 'REPLACE_ME',
  storageBucket: 'REPLACE_ME.appspot.com',
  messagingSenderId: 'REPLACE_ME',
  appId: 'REPLACE_ME',
};
